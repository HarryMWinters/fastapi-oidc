

on:
  push:
    branches:
      - master
  workflow_dispatch:

jobs:
  build_doc:
    runs-on: ubuntu-latest

    steps:
      - name: Install Task
        uses: Arduino/actions/setup-taskfile@master
        with:
          version: 3.x
      - name: Build Docs
        run: task build_docs
      - name: Publish Docs
        run: echo "Publishing..."
